"""Setup shim for legacy tooling.

All package metadata, dependencies, entry points, and package data are
declared in pyproject.toml — edit that file, not this one.
"""
from setuptools import setup

setup()
